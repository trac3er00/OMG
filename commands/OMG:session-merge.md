---
description: "[DEPRECATED] Use /OMG:session merge instead."
allowed-tools: Read
---

# /OMG:session-merge — DEPRECATED

This command has been merged into `/OMG:session`.

```
/OMG:session merge --from "experiment" --into "main"
/OMG:session merge --from "experiment" --preview
```
