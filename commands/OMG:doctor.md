---
description: "[DEPRECATED] Use /OMG:validate doctor instead."
allowed-tools: Read
---

# /OMG:doctor — DEPRECATED

This command has been merged into `/OMG:validate`.

Run `/OMG:validate doctor` for runtime and install verification.

```
/OMG:validate doctor        # Same as old /OMG:doctor
/OMG:validate doctor --fix  # Auto-repair known issues
/OMG:validate               # Full validation (doctor + health + plugins + contracts)
```
