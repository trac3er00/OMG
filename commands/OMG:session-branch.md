---
description: "[DEPRECATED] Use /OMG:session branch instead."
allowed-tools: Read
---

# /OMG:session-branch — DEPRECATED

This command has been merged into `/OMG:session`.

```
/OMG:session branch --name "experiment"    # Same as old /OMG:session-branch
/OMG:session fork --from <id> --name <n>   # Same as old /OMG:session-fork
/OMG:session merge --from <branch>         # Same as old /OMG:session-merge
```
