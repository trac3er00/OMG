---
description: "[DEPRECATED] Use /OMG:stats cost instead."
allowed-tools: Read
---

# /OMG:cost — DEPRECATED

This command has been merged into `/OMG:stats`.

```
/OMG:stats cost             # Same as old /OMG:cost
/OMG:stats cost history     # Same as old /OMG:cost history
/OMG:stats cost budget      # Same as old /OMG:cost budget
/OMG:stats cost reset       # Same as old /OMG:cost reset
/OMG:stats                  # Full session analytics
```
