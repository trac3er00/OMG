---
description: "[DEPRECATED] Use /OMG:validate health instead."
allowed-tools: Read
---

# /OMG:health-check — DEPRECATED

This command has been merged into `/OMG:validate`.

Run `/OMG:validate health` for project setup and context health checks.

```
/OMG:validate health    # Same as old /OMG:health-check
/OMG:validate           # Full validation (doctor + health + plugins + contracts)
```
