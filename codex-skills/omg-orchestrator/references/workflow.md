# OMG Orchestrator Reference

- Start with repository context, not assumptions.
- Prefer `teams --target auto` only when the user did not choose a provider.
- Prefer `ccg` for mixed backend/frontend requests; add `kimi` only when long-context synthesis is explicitly useful.
- Keep outputs structured as: findings, implementation path, verification, residual risk.
