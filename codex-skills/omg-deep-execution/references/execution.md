# Codex-First Execution

- Use this path for backend logic, debugging, algorithms, and security-sensitive implementation.
- When UI concerns dominate, switch to `omg-provider-interop` and route Gemini instead of forcing Codex through visual work.
- Preserve exact verification commands as you go so `omg-verified-delivery` can reuse them.
