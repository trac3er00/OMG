Task 11 (F10) Evidence — Knowledge index validation
Date: 2026-02-28
File: hooks/prompt-enhancer.py

Verification commands and outputs:
1) python3 -m py_compile hooks/prompt-enhancer.py
   - exit: 0 (no output)

2) grep -c 'isinstance.*dict' hooks/prompt-enhancer.py
   - output: 3

3) grep -n "isinstance(index, dict)\|index.json is not a dict\|os.remove(index_path)" hooks/prompt-enhancer.py
   - 449:                if not isinstance(index, dict):
   - 450:                    print(f"[OAL] prompt-enhancer: index.json is not a dict ({type(index).__name__}), rebuilding", file=sys.stderr)
   - 452:                        os.remove(index_path)

Behavior summary:
- After loading .index.json, code now validates `index` type.
- If `index` is not a dict: emits stderr warning, removes invalid index file, and resets to `{}` to trigger rebuild.
- Existing corrupted JSON handling remains intact.
