Task 25: Ralph Prompt Re-Injection + Escape Hatch
==================================================
Date: 2026-02-28

## Files Modified
1. hooks/stop_dispatcher.py — added format_ralph_block_reason(), updated check_ralph_loop()
2. hooks/prompt-enhancer.py — added get_feature_flag import, ralph/ulw keyword auto-creates ralph-loop.json
3. commands/OMG:ralph-start.md — new command file (44 lines)
4. commands/OMG:ralph-stop.md — new command file (24 lines)
5. tests/test_ralph.py — extended from 5 to 9 tests

## Acceptance Criteria

### A. format_ralph_block_reason() added to stop_dispatcher.py
✓ Function builds rich reason string with iteration count, checklist progress, original prompt, and stop instruction
✓ check_ralph_loop() updated to call format_ralph_block_reason() instead of inline f-string
✓ Checklist progress parsed from checklist_path in state, regex matches [x] and [ ]/[!] patterns

### B. commands/OMG:ralph-start.md created
✓ File exists at commands/OMG:ralph-start.md
✓ Has YAML frontmatter (description, allowed-tools, argument-hint)
✓ Documents state file format, usage, and stop conditions

### C. commands/OMG:ralph-stop.md created
✓ File exists at commands/OMG:ralph-stop.md
✓ Has YAML frontmatter
✓ Documents deactivation behavior and no-active-loop message

### D. prompt-enhancer.py keyword activation
✓ get_feature_flag imported from _common
✓ After ULW/ralph mode detection, auto-creates .omg/state/ralph-loop.json if:
  - ralph/ulw/ultrawork keyword detected (is_ulw=True)
  - ralph_loop feature flag enabled
  - ralph-loop.json does not already exist
✓ Gomg extracted from prompt text after keyword, capped at 200 chars
✓ State includes: active, iteration, max_iterations, original_prompt, started_at, checklist_path

### E. Tests added to tests/test_ralph.py
✓ test_ralph_block_reason_includes_progress: Creates checklist 2/4 done, asserts "Progress: 2/4" in reason
✓ test_ralph_block_reason_includes_original_prompt: Asserts original_prompt text in reason
✓ test_ralph_block_reason_includes_stop_instruction: Asserts "/OMG:ralph-stop" in reason
✓ test_ralph_commands_exist: Asserts both command files exist

## Verification

### Syntax check
$ python3 -m py_compile hooks/stop_dispatcher.py → OK
$ python3 -m py_compile hooks/prompt-enhancer.py → OK

### Ralph tests
$ python3 -m pytest tests/test_ralph.py -x -v
9 passed in 0.84s

### Full test suite
$ python3 -m pytest tests/ -x -q --ignore=tests/e2e
314 passed in 9.93s

### LSP diagnostics
- hooks/stop_dispatcher.py: No errors
- hooks/prompt-enhancer.py: No errors
- tests/test_ralph.py: No errors

## Design Notes
- _run_dispatcher helper updated to disable OMG_PLANNING_ENFORCEMENT_ENABLED by default,
  isolating ralph tests from cross-talk with planning gate checks
- format_ralph_block_reason uses state.get('iteration') AFTER increment (shows current iteration)
- Checklist progress only shown when checklist_path is set AND file exists
- datetime import is inline (from datetime import datetime as _dt) to avoid module-level side effects
