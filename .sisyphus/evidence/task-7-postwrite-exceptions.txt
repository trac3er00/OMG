# Task 7 — Harden exception handlers in hooks/post-write.py
# Date: 2026-02-28

## Verification

### py_compile: PASS
$ python3 -m py_compile hooks/post-write.py → exit 0

### AST check for `except Exception: pass`: 0 matches
$ python3 -c (AST walk) → "Total except Exception: pass blocks: 0"

### grep for `except Exception`: only `as e` variants remain
  Line 73: except Exception as e:  (security: file read for secret scan)
  Line 168: except Exception as e: (security: signal file write)

### Exit codes: all sys.exit(0), crash isolation preserved
  Lines 16,20,28,70,75,80,181,199: all sys.exit(0)

## Changes Summary

### BEFORE: 3 silent handlers
1. Line 44-45: `except Exception: pass` (QG config loading)
2. Line 74-75: `except Exception: sys.exit(0)` (file read for secret scan — silent skip)
3. Line 168-169: `except Exception: pass` (signal file write — SECURITY-CRITICAL)

### AFTER: 0 silent handlers in security paths
1. Line 38: `with contextlib.suppress(Exception):  # intentional: cleanup`
   → Config loading failure = format stays disabled (safe default)
2. Line 73-75: `except Exception as e:` + stderr log + sys.exit(0)
   → File read failure now logged before graceful exit
3. Line 168-169: `except Exception as e:` + stderr log
   → Signal file write failure now visible in stderr

### Import added
- `import contextlib` (line 8) — stdlib only

### Handler classification
| Handler | Path | Classification | Action |
|---------|------|----------------|--------|
| L44 (was) | QG config load | Cleanup | contextlib.suppress + comment |
| L62 | Formatter subprocess | Cleanup | Already specific exceptions, untouched |
| L73 | File read for scan | Security | Added stderr logging |
| L168 | Signal file write | Security-critical | Added stderr logging |

### Untouched (correctly scoped exceptions)
- L15: except (json.JSONDecodeError, EOFError) — specific, fine
- L62: except (FileNotFoundError, subprocess.TimeoutExpired, OSError): pass — specific, cleanup

### Delta
- Before: 2 `except Exception: pass`, 1 `except Exception: sys.exit(0)` (silent)
- After: 0 bare `except Exception`, 2 logged security handlers, 1 contextlib.suppress cleanup
