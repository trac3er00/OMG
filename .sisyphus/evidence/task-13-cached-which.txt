TASK 13: Cache shutil.which result in runtime/team_router.py

OBJECTIVE:
Reduce redundant calls to _check_tool_available(tool_bin) in dispatch_team() function.

CHANGES MADE:
1. Line 126: Introduced local variable `tool_available` to cache the result
   - Before: if tool_bin and not _check_tool_available(tool_bin):
   - After:  tool_available = _check_tool_available(tool_bin) if tool_bin else None
            if tool_bin and not tool_available:

2. Line 160: Replaced second call with cached variable
   - Before: "tool_available": _check_tool_available(tool_bin) if tool_bin else None,
   - After:  "tool_available": tool_available,

VERIFICATION:
✓ python3 -m py_compile runtime/team_router.py → exit 0
✓ grep -c '_check_tool_available' → 4 (reduced from 5)
  - 1 function definition (line 84)
  - 3 remaining call sites (lines 194, 216 in other functions)
  - 0 redundant calls in dispatch_team()

IMPACT:
- Eliminates one redundant shutil.which() call per dispatch_team() invocation
- Maintains identical behavior (same logic, same return value)
- No external dependencies added
- No changes to function signature or public API
