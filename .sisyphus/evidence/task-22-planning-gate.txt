Task 22 Evidence: Planning Completion Gate (P2)
Date: 2026-02-28

Changed files:
- hooks/stop_dispatcher.py
- tests/test_planning_gate.py

Implementation details:
- Added check_planning_gate(project_dir):
  - Feature flag guard via get_feature_flag("planning_enforcement")
  - Reads checklist through resolve_state_file(project_dir, "state/_checklist.md", "_checklist.md")
  - Computes total/done/blocked/pending using checklist regex markers
  - Blocks completion when pending > 0 with planning-gate reason
- Added advisory-only check_scope_drift(project_dir):
  - Uses git diff --name-only HEAD
  - Loads plan via resolve_state_file(project_dir, "state/_plan.md", "_plan.md")
  - Calculates changed files not mentioned in plan
  - Emits advisory when outside-plan ratio > 30%
- Replaced P2 main stub to:
  - Execute check_planning_gate and block on first block reason
  - Extend advisories with check_scope_drift output
  - Append advisories into _stop_advisories for stderr output
- Added tests/test_planning_gate.py with required scenarios:
  - incomplete checklist blocks
  - complete checklist allows
  - [!] blocked items not counted as pending
  - missing checklist allows

Diagnostics:
- lsp_diagnostics clean (error severity) for changed files:
  - hooks/stop_dispatcher.py
  - tests/test_planning_gate.py

Verification:
1) Targeted tests:
- Command: python3 -m pytest tests/test_planning_gate.py tests/test_stop_dispatcher.py tests/test_ralph.py -x -q
- Result: 19 passed

2) Full suite:
- Command: python3 -m pytest tests/ -x -q --ignore=tests/e2e
- Result: 291 passed
