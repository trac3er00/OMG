F2 Code Quality Review — OMG Remediation
=========================================
Date: 2026-02-28
Reviewer: Sisyphus-Junior (automated)

═══════════════════════════════════════════
1. COMPILE CHECKS
═══════════════════════════════════════════

  hooks/post-write.py        PASS
  hooks/stop_dispatcher.py   PASS
  hooks/config-guard.py      PASS
  hooks/_common.py           PASS
  hooks/pre-compact.py       PASS
  hooks/shadow_manager.py    PASS
  hooks/prompt-enhancer.py   PASS
  hooks/session-start.py     PASS
  hooks/circuit-breaker.py   PASS
  hooks/test-validator.py    PASS
  hooks/quality-runner.py    PASS
  hooks/pre-tool-inject.py   PASS
  control_plane/server.py    PASS
  runtime/team_router.py     PASS

  bash -n OMG-setup.sh       PASS

  Result: 14/14 Python + 1/1 Shell = ALL PASS

═══════════════════════════════════════════
2. EMPTY CATCHES IN SECURITY FILES
═══════════════════════════════════════════

  Target files (T7/T8/T9 remediation):
    post-write.py:     0 broad empty catches (formatter uses specific OSError/FileNotFoundError)
    stop_dispatcher.py: 0 broad empty catches (all except Exception have logging now)
    config-guard.py:   0 broad empty catches (all have [OMG] logging)

  Non-security files with empty except Exception: pass:
    _common.py:        4 (all in crash-isolation contexts with comments)
    pre-compact.py:    3 (pre-existing, not in T7/T8/T9 scope)
    shadow_manager.py: 2 (pre-existing)
    prompt-enhancer.py: 10 (pre-existing, only T15 import change)
    session-start.py:  3 (pre-existing)
    circuit-breaker.py: 5 (pre-existing)
    test-validator.py: 1 (pre-existing)
    pre-tool-inject.py: 1 (pre-existing)

  Verdict: Security files CLEAN. Non-security files have pre-existing empty catches
           outside remediation scope — not blocking.

═══════════════════════════════════════════
3. print() WITHOUT file=sys.stderr
═══════════════════════════════════════════

  Method: AST analysis (handles multi-line print calls)
  Result: 0 violations — all print() calls in hooks use file=sys.stderr

═══════════════════════════════════════════
4. contextlib IMPORT VERIFICATION
═══════════════════════════════════════════

  hooks/post-write.py:  import contextlib ✓  uses: contextlib.suppress(Exception) L38
  hooks/config-guard.py: import contextlib ✓  uses: contextlib.suppress(OSError) L139
  hooks/stop_dispatcher.py: no contextlib usage — no import needed ✓

═══════════════════════════════════════════
5. EXCEPTION HANDLER LOG MESSAGE PATTERN
═══════════════════════════════════════════

  Expected: [OMG] <file>: {type(e).__name__}: {e}

  post-write.py:      2 handlers — all match pattern ✓
  stop_dispatcher.py:  8 handlers — all match pattern ✓
  config-guard.py:     3 handlers — all match pattern (with context prefix) ✓
  _common.py:          4 handlers — all match pattern ✓

═══════════════════════════════════════════
6. UNUSED IMPORTS
═══════════════════════════════════════════

  _resolve_project_dir: imported in 11 files, used in all 11 ✓
  timezone:            imported where needed, used in all importing files ✓
  contextlib:          imported only where used (2 files) ✓
  No unused imports detected in any modified file.

═══════════════════════════════════════════
7. SPOT-CHECK FINDINGS
═══════════════════════════════════════════

  post-write.py:
    - Secret scan logic well-structured
    - Signal file write wrapped in try/except with [OMG] logging ✓
    - contextlib.suppress for non-critical format config read ✓
    - No AI slop detected

  stop_dispatcher.py:
    - All 8 except Exception handlers log to stderr ✓
    - Intentional bare catches (json.JSONDecodeError, ValueError, OSError) have
      "# intentional:" comments explaining rationale ✓
    - timezone.utc used for time comparisons (L152) ✓

  _common.py:
    - Write-path handlers (T10) properly structured ✓
    - 4 instances of redundant `pass` after `print(..., file=sys.stderr)` (style nit)
    - Crash-isolation empty catches in error logger justified (avoid recursion)

  control_plane/server.py:
    - Host warning present when binding to non-127.0.0.1 (T12) ✓
    - Warning uses file=sys.stderr ✓

  runtime/team_router.py:
    - shutil.which used for tool availability check ✓
    - NOTE: No result caching observed — _check_tool_available called 3x per
      invocation without memoization. T13 caching may be incomplete.
      (Low severity: shutil.which is fast, ~3 calls per hook run)

═══════════════════════════════════════════
8. OBSERVATIONS (non-blocking)
═══════════════════════════════════════════

  [STYLE] _common.py: 4x redundant `pass` after print() in except blocks
  [STYLE] team_router.py: shutil.which not cached (T13 may be incomplete)
  [INFO]  Non-security hooks have ~25 pre-existing empty catches outside
          remediation scope — future cleanup opportunity

═══════════════════════════════════════════
SUMMARY
═══════════════════════════════════════════

  Compile:           PASS (15/15)
  Empty catches:     0 in security files
  Bare print():      0 violations
  Unused imports:    0 found
  Handler patterns:  17/17 match [OMG] format
  contextlib:        Correctly imported where used

  Issues found: 0 blocking, 2 style nits, 1 possible incomplete task (T13 cache)

═══════════════════════════════════════════

  Compile [PASS] | Issues [0 blocking, 3 observations] | VERDICT: APPROVE
