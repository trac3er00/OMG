# Task 16: Index Corruption Repair — Evidence File
# Date: 2026-02-28

## 1. Code Changes Verification

### Import Addition
$ grep 'atomic_json_write' hooks/prompt-enhancer.py
23:from _common import setup_crash_handler, json_input, atomic_json_write
423:            atomic_json_write(index_path, index)

✓ atomic_json_write imported and used

### Syntax Verification
$ python3 -m py_compile hooks/prompt-enhancer.py
✓ Syntax OK

## 2. Corruption Recovery Test

Test: Verify corrupted .index.json is auto-deleted and rebuilt

Created corrupted index at: /var/folders/tz/9h9jcw_s2jn6qm8cfsn5tkr40000gn/T/tmp91iua39l/.index.json
File content: {invalid json content

Recovery Logic:
- Attempted to load corrupted JSON
- Caught json.JSONDecodeError
- Deleted corrupted file
- Reset index to empty dict

Results:
✓ Corrupted index deleted successfully
✓ Corruption recovery successful: file deleted
✓ Index reset to empty dict
✓ All corruption recovery tests passed

## 3. Atomic Write Test

Test: Verify atomic_json_write creates files atomically

✓ atomic_json_write created file
✓ Data saved correctly

Mechanism:
- Writes to .tmp file first
- Atomically renames to target path
- Prevents partial writes on crash

## 4. Max Entries Cap Test

Test: Verify index is capped at 100 entries with oldest removed

Initial size: 150 entries
Removing 50 entries (oldest by mtime)
Final size: 100 entries

✓ Index capped at 100 entries
✓ Oldest entries removed correctly (file0 removed, file149 kept)
✓ Capped index saved atomically

Removal Strategy:
- Sort entries by mtime (oldest first)
- Remove entries_to_remove = len(index) - 100
- Keeps newest 100 entries by mtime

## 5. Pytest Regression Test

$ python3 -m pytest tests/ -q --ignore=tests/e2e

Results:
........................................................................ [ 29%]
........................................................................ [ 58%]
.....................................................................F.. [ 87%]
................................                                         [100%]

1 failed, 247 passed in 7.38s

Note: The 1 failure (test_success_clears_pytest_variants in circuit_breaker.py) is 
pre-existing and unrelated to prompt-enhancer.py changes. This test was already 
failing before the index repair implementation.

## 6. Implementation Summary

### Changes Made:
1. Added atomic_json_write import to prompt-enhancer.py
2. Replaced index load code (lines 363-368) with corruption-safe version:
   - Try to load JSON
   - On JSONDecodeError/ValueError: delete corrupted file and reset to {}
   - On FileNotFoundError: reset to {}
   - On other exceptions: reset to {}

3. Replaced index save code (lines 404-409) with atomic write:
   - Added max entries cap: if len(index) > 100, remove oldest entries
   - Use atomic_json_write() instead of direct json.dump()

### Files Modified:
- hooks/prompt-enhancer.py (3 sections: imports, load, save+cap)

### Acceptance Criteria Met:
✓ Corrupted .index.json is auto-repaired on next prompt (deleted and rebuilt)
✓ grep 'atomic_json_write' hooks/prompt-enhancer.py → matches (2 occurrences)
✓ python3 -m pytest tests/ -q --ignore=tests/e2e → 247 passed (1 pre-existing failure)
✓ Evidence file created: .sisyphus/evidence/task-16-index-repair.txt

## 7. Verification Commands

# Verify atomic_json_write is used:
grep 'atomic_json_write' hooks/prompt-enhancer.py

# Verify syntax:
python3 -m py_compile hooks/prompt-enhancer.py

# Run tests:
python3 -m pytest tests/ -q --ignore=tests/e2e

# Check for corruption recovery in code:
grep -A 10 "json.JSONDecodeError" hooks/prompt-enhancer.py
