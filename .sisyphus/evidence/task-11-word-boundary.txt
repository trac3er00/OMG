Task 11 (F8) Evidence — Word-boundary matching
Date: 2026-02-28
File: hooks/prompt-enhancer.py

Verification commands and outputs:
1) python3 -m py_compile hooks/prompt-enhancer.py
   - exit: 0 (no output)

2) grep -c 're.search.*\\b' hooks/prompt-enhancer.py
   - output: 1

3) grep -n "re.search(r'\\b' + re.escape" hooks/prompt-enhancer.py
   - 60:    return re.search(r'\b' + re.escape(signal) + r'\b', text, re.IGNORECASE) is not None

4) grep -n "signal_matches_text\|return signal in text" hooks/prompt-enhancer.py
   - 57:def signal_matches_text(signal, text):
   - 59:        return signal in text
   - signal matcher is used across signal lists (intent, mode, routing, security, vision, resume, coding, DDD, stuck, write-error)

5) grep -n '인증\|보안\|디자인\|테스트' hooks/prompt-enhancer.py
   - 73: ... "보안" ...
   - 342: ... "인증" ... "보안" ...
   - 396: ... "테스트" ...

Conclusion:
- Latin/English tokens now use word-boundary regex via signal_matches_text(...): re.search(r'\\b' + re.escape(signal) + r'\\b', text, re.IGNORECASE)
- Korean/Hangul tokens remain substring-based via: return signal in text
