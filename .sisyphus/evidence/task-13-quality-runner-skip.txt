# Task 13: Context Pressure Short-Circuit in quality-runner.py

## Objective
Add context pressure check to skip 60-second subprocess execution when context pressure is high.

## Implementation
File: hooks/quality-runner.py
Location: After project_dir resolution (line 174), before check_quality_runner() call

Added code:
```python
# Short-circuit: skip subprocess if context pressure is high
_pressure_path = os.path.join(project_dir, ".oal", "state", ".context-pressure.json")
try:
    if os.path.exists(_pressure_path):
        with open(_pressure_path, "r") as _f:
            _pressure = json.load(_f)
        if _pressure.get("is_high", False):
            print("[OAL quality-runner] Skipping subprocess checks: context pressure high", file=sys.stderr)
            sys.exit(0)
except Exception:
    pass  # fail open — run checks if pressure file unreadable
```

## Verification Results

### 1. Syntax Check
Command: python3 -m py_compile hooks/quality-runner.py
Result: ✓ PASS (exit 0)

### 2. High Pressure Scenario
Setup:
- .oal/state/.context-pressure.json: {"tool_count": 200, "threshold": 150, "is_high": true}
- .oal/state/quality-gate.json: {"test":"echo pass"}

Command: echo '{"stop_hook_active":false,"transcript_path":"/dev/null"}' | python3 hooks/quality-runner.py 2>&1
Result: ✓ PASS
Output: [OAL quality-runner] Skipping subprocess checks: context pressure high
- Subprocess execution was skipped
- No quality gate checks ran
- Exit code: 0

### 3. Normal Pressure Scenario (Subprocess Still Runs)
Setup:
- .oal/state/.context-pressure.json: {"tool_count": 100, "threshold": 150, "is_high": false}
- .oal/state/quality-gate.json: {"test":"echo pass"}

Command: echo '{"stop_hook_active":false,"transcript_path":"/dev/null"}' | python3 hooks/quality-runner.py 2>&1
Result: ✓ PASS
Output: Subprocess ran and executed quality gate checks (failed as expected due to invalid command)
- Pressure check did NOT skip
- Quality gate subprocess executed normally
- Exit code: 0

## Summary
✓ Context pressure check implemented
✓ Subprocess skipped when is_high=true
✓ Subprocess runs normally when is_high=false
✓ Log message matches spec: "[OAL quality-runner] Skipping subprocess checks: context pressure high"
✓ Python syntax valid
✓ Fail-open behavior: if pressure file unreadable, subprocess runs
