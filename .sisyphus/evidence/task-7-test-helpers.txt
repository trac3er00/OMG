# Task 7: Test Helpers — Evidence

## File Created
✓ tests/helpers.py (67 lines)

## Import Verification
Command: python3 -c "import sys; sys.path.insert(0,'.'); from tests.helpers import run_hook, make_ledger_entry, setup_state, assert_injection_contains, assert_injection_under_budget; print('✓ All imports successful')"
Output: ✓ All imports successful
Exit Code: 0

## Functions Implemented
1. run_hook(hook_name, input_data, env=None, cwd=None)
   - Runs hook via subprocess with JSON stdin
   - Parses JSON output or returns raw string
   - Returns (stdout, stderr, exit_code)

2. make_ledger_entry(tool, command=None, exit_code=0, file=None, success=True)
   - Creates tool-ledger.jsonl entry dict
   - Supports optional command, file, exit_code, success fields

3. setup_state(tmp_dir, files_dict)
   - Creates state files from dict {relative_path: content}
   - Handles JSON serialization for dict/list content
   - Creates parent directories as needed

4. assert_injection_contains(output, *keywords)
   - Asserts contextInjection contains all keywords
   - Works with dict or string output
   - Provides helpful error messages

5. assert_injection_under_budget(output, max_chars)
   - Asserts contextInjection length within budget
   - Works with dict or string output
   - Reports actual vs max length on failure

## Test Suite Verification
Command: python3 -m pytest tests/ -x -q --ignore=tests/e2e
Output: 199 passed in 5.51s
Exit Code: 0

## Complementarity Check
✓ helpers.py complements conftest.py fixtures (tmp_project, mock_stdin, clean_env)
✓ No duplicate functionality
✓ No external dependencies added
✓ No existing files modified

## Status
✓ COMPLETE — All acceptance criteria met
